[Unit]
Description=Claw in the Box image updater
After=docker.service network-online.target
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c '\
  /usr/bin/docker pull ghcr.io/claw-in-the-box-ai/cloud:stable && \
  /usr/bin/systemctl restart clawinthebox && \
  rm -f /data/update.trigger'
